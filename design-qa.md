# V4 連續導引：介面驗證

final result: passed

## 2026-09-08 步進／自動接續增補

- 維持原有相機、藍色路線、末端皮卡與旋轉小地圖，新增底部「自動計步／手動加步」選擇及手動「＋一步」。校準與試用接續說明仍收在資訊選單；自動接續預設關閉，入口提供勾選。
- 新證據：`test-output/step-flow/manual-390x844.png`、`manual-360x844.png`、`manual-1280x844.png`、`manual-844x390.png`。已檢視 390 直向與 844×390 橫向實際瀏覽器截圖；底部操作與使用者地圖標記未互相遮擋，按鈕／選擇最少 44px。背景為受控測試的空白相機畫面，不是現場辨識精度證據。
- 底部工具區由 ResizeObserver 量測實際高度，地圖保留相應空間；手機相機／地圖比例依新增操作調整，短橫向仍為左右配置，不固定套用舊 68/32 比例。
- 初版測試發現 QR 預覽的合成起點 `manual-origin` 不應顯示為已確認節點，已改用對應真實節點。獨立檢查另修正：校準被中斷仍須返回位置確認、校準中禁止返回上一段，以及同節點重新确认後視覺更新不得讀取舊閉包。
- 新增 10 組合成瀏覽器情境與 38 項步進／接續單元測試，包含手動防連點、手動／感測互斥、三檔十步校準、未同意不套用設定、推估接續不改最後確認位置、只看遠處照片或只加步不接續，以及權限拒絕退路。最終執行以 `test-output/step-flow/report.json` 為準，報告比對執行前後 bundle hash 防止混版。
- 新試用模式可「自動推估接續」，舊文以下的「不自動抵達」仍指不自動把推估當成使用者已確認抵達。跨樓層與目的地保留確認；位置／距離與計步准确度仍待真實手機現場驗收。

## 比對基準

- 日期：2026-09-08；範圍：既有 V4 民眾相機導引，不是新建網站，也不改 V3。
- Source visual truth：`/Volumes/MyPassport/CodexHome/generated_images/01a06a11-6152-7331-810b-741f71af1340/exec-68994ca1-fbfc-4853-ac65-32b254ff1549.png`。
- 原始參考圖 853 × 1844 px，正規化副本 `test-output/guidance-qa/reference-normalized.png` 為 390 × 844 px。CSS viewport 390 × 844、deviceScaleFactor 1；只做密度／尺寸正規化。
- 實作：`test-output/guidance-qa/active-390.png`，390 × 844 px；同次比對輸入並列開啟參考與實作圖。
- 同狀態：直向手機、已建立方向、下一端點地圖距離 8m、藍色路線、末端皮卡、下方小地圖。參考為概念圖；實作是合成相機影像經真正前端渲染，並非實地精度驗證。
- 預覽 `http://127.0.0.1:8081/ar-v4-navigation.html?v=continuous-guide-20260908` 使用既有独立本機資料。QA 使用獨立臨時路網與既有生成圖書館環景；未寫入正式場域。

## Findings 與修正歷程

1. [P2，已修正] 寬畫面小地圖裁掉使用者。初版固定 600 × 240 viewBox 搭配 slice，桌面可視 y 範圍不包含使用者 anchor。改成量測實際 SVG 尺寸，以已旋轉的剩餘路線範圍自動配置倍率與中心；9 組收合／展開／橫向標記邊界測試通過。修正後證據：`test-output/guidance/estimated-1280.png`、`test-output/guidance-qa/active-1280.png`。
2. [P2，已修正] 相機帶狀路線每個細分矩形都畫白框，產生橫向接縫。改為實際路線側邊的獨立裁切白線，填色不畫內框；方向符號採真正路徑上的三角箭頭。修正後證據：`test-output/guidance-qa/active-390.png`。
3. [P2，已修正] 吉祥物透明留白造成可見本體過小。依原始資產透明邊界調整呈現窗口，保留原資產與 V3；腳底對齊投影端點及藍色端點圈。相機／小地圖都使用相同既有皮卡資產。修正後證據：`test-output/guidance-qa/active-390.png`。
4. [P2，已修正] 844 × 390 橫向畫面被 min-height 撐到 420px，提示互相重疊。短橫向改成相機／地圖左右配置，不需要滾動找控制項。修正後證據：`test-output/guidance-qa/active-844.png`（844 × 390 px）。
5. [P2，已修正] 360 × 740 時狀態條接近端點標籤。狀態條上移至標頭下方、低高度導引時隱藏重複文字，保留底部狀態與右上說明入口。修正後證據：`test-output/guidance-qa/active-360.png`。

最終再次同時開啟正規化參考與 390／360 實作圖，並檢視 1280 與橫向圖；未留下阻擋使用的 P0/P1/P2。

## 五項必查表面

- 字型與排版：沿用既有系統中文字型與較粗的導引標題。主行動字級 14–19px，次資訊縮小；主按鈕、地標名稱可讀。真實地圖底圖本身的文字在小視窗較小，保留地圖展開鈕。原參考的精確字型不可取得，未擅自下載新字型。
- 版面節奏：相機上方、地圖下方約 68/32 配置，保留圓角接合、底部抵達操作；短橫向改左右。頁面沒有水平溢出，位置箭頭未被 footer 蓋住。照片狀態、近似標示與人工退路是實際功能需要，較概念图增加少量資訊。
- 色彩：沿用深綠 #12322f／#214b3d 與淺綠白介面，路徑為藍 #147bec、白描邊。未更動工作台或 V3 色彩。
- 影像品質：重用透明皮卡 PNG，不用 emoji 假裝手部動作；相機背景是實際 video 元件。截圖的圖書館只由測試注入，正式前端沒有固定背景。路線 SVG、地圖標記為實際節點／感測資料的圖形呈現，不是拿程式仿畫場景插畫。
- 文案：區分「照片已定向／人工定向／沿路推估／方向待校正」；保留「非精確空間錨定」、估計步長、人工抵達，沒有聲稱達到 Google 定位精度。說明、所有既有診斷與校正操作收在資訊對話框。

全圖已足以辨識主要視覺層級；另以實作 390px 原尺寸逐一讀取端點文字、底部方向／距離、兩個位置標記及資訊對話框（`test-output/guidance-qa/help-390.png`）。這些為實際畫面，不使用程式碼推測可見結果。

## 互動、存取與限制

- 已驗證手機／桌面、展開地圖、向左／右／後方提示、初始人工或視覺定向、步伐推估、步長修改、明確抵達、回到路線、權限拒絕與舊資料不復活。
- 對話框用原生 dialog，保留關閉鈕、Escape 與焦點控制。主要按钮最少 44px；遵守 reduced-motion。
- 原有民眾端回歸與視覺定向回歸通過。新連續引導測試 39 項單元 + 9 組瀏覽器情境；瀏覽器 console pageerror 為 0。
- 證據：`test-output/guidance/report.json`、`test-output/visual-heading/report.json`、`test-output/guidance-qa/report.json`。
- 實機 iPhone／Android、Safari／LINE、陀螺儀噪聲、磁場與步長誤差尚需現場驗證。UI QA 通過不代表世界座標錨定或定位精度通過。
- 路線形状、端點大小與位置依真實數值投影；不為模仿概念圖而硬移動端點。底圖保留既有場域，不用概念圖中的假平面圖替換。

## Implementation Checklist

- [x] 共用位置、朝向、路段給相機路線與小地圖。
- [x] 端點在鏡頭外不假造螢幕座標。
- [x] 影像漏判不阻止已建立的近似導引；保留失效／預算限制。
- [x] 步伐只推估沿路進度，不自動抵達。
- [x] 手機、桌面、橫向與收合／展開畫面驗證。
- [x] V3 與現有場域資料無修改。

## Follow-up Polish

- P3：有合適的正式揮手動作素材時可換上；目前沿用既有皮卡，未新增骨架／3D 動畫。
- P3：可另做可讀性更好的專用簡化小地圖底圖，但不在本次覆寫現有平面圖。
