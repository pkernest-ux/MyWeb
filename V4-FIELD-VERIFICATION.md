# V4 本機試作驗證紀錄

驗證日期：2026-09-05。以下為本機開發階段的驗證紀錄，不代表雲端部署完成；後續發佈狀態須以 GitHub 提交、CI 與實際站台驗證為準。

## 相機頁整合導航流程測試

後續路段聚焦更新：V4 改用獨立 `ar-v4-navigation.html` 與 `assets/ar-v4-navigation/`，僅在 V4 入口啟用目前路段縮放。新增 320px、390px、1200px 視口幾何測試（含直線、折線、短段、角落單點、空資料），確認端點位於安全範圍；74 項 Node 測試通過。原 V3 HTML、JS、CSS、Worker SHA-256 與修改前一致。共用來源增加預設關閉的 opt-in 參數，AR 畫面未修改。手機實際畫面、跨樓層現場體驗仍待瀏覽器／實機驗收。本機服務新增精確入口白名單後重啟，未放寬原始碼或資料路徑存取。

- 「相機」頁增加「照片與方向／導航流程」切換，內嵌原 V3 導航頁，不增加底部頁籤。不自動帶入工作台場域／節點，須在導航頁選擇；仍使用已保存路網與原 V3 辨識流程。
- 啟動前關閉採集鏡頭與辨識，防止相機競爭；離開相機頁、返回照片模式或變更場域／保存版本時卸載導航 iframe。未保存路網或後台忙碌時禁止啟動。照片與路網草稿保持原流程。
- 73／73 項 Node 測試、V4 型別檢查及建置通過。新增測試實際執行啟動回呼、檢查 16 組導航掛載條件及入口；本機 V4 資產與 V3 入口／JS 皆 HTTP 200 且與檔案一致。
- 本次未執行瀏覽器實測；內嵌導航在手機的相機／方向授權及實際路線仍待驗收。未推送 GitHub，未修改場域資料，也未宣稱完整 V4 民眾導航已完成。

## 路網工具分類與折疊選單（目前本機版本）

- 後續色調統一：新增僅限 V4 的淺底綠色主題，涵蓋分類選單、地圖周邊、輸入欄位、點位設定和確認視窗；原始平面圖與路線顏色不改。最新 72／72 項 Node 測試通過，新增主題範圍與七組文字色對比（至少 4.5:1）檢查。已重建本機資產，尚未進行本次主題的瀏覽器截圖或手機實機驗證。
- 路網工具改為五組單開折疊分類：「樓層與底圖、路徑節點、AR 點位、路網測試、資料保存」。分類展開／收合只改呈現，不會取消操作模式或刪除草稿。
- 頂部持續顯示專案、建物、樓層、目前模式與未保存狀態；「切換樓層」直接打開樓層分類。「工具選單／收合工具」可收起全部分類，手機啟用操作模式後自動收合，保留「結束操作」。
- 既有路徑節點和 AR 點新增選取清單；點位設定面板保留可捲動表單與完成／關閉入口。載入後台失敗時自動展開資料保存並提示復原方式。
- 父頁操作說明改為可折疊，提供「回位置採集」，且切頁不卸載編輯器。修正路網頁意外顯示空白工具卡，避免額外占用手機高度。
- V4 獨立 TypeScript 檢查、AR 編輯器及 V4 生產資產建置通過。完整專案仍有舊版 TypeScript 問題，未宣稱全庫型別檢查通過。
- 新增 `test/ar-v4-editor-layout.test.mjs`，透過實際 JSX 的伺服器端渲染與抽取實際回呼執行，檢查分類／ARIA、互斥模式、折疊後焦點、地圖控制不誤建節點、跨樓層測試與父頁草稿保留；不啟動瀏覽器或相機。
- 最終 71／71 項 Node 自動測試通過（原有 60 項、新增 11 項）。包含 V4 節點長按／右鍵僅開啟設定、不直接刪除或變更跨樓層設定；V3 原右鍵行為保留。此安全修正後已重新建置並確認本機提供最新 AR 資產。
- 本機 8080 的兩個入口與四個 JS／CSS 資產 HTTP 200，回傳內容 SHA-256 與建置檔一致；`storage=local`、`projectWritesEnabled=true`。兩份場域資料 SHA-256 維持 `3693cdce2b9b91d88ab327a5f33560fb8a304bcb7b303f15f7568288f8d0b2f5`，未修改使用者資料。
- **本輪未執行瀏覽器、截圖或手機實機驗證。** 下方路網瀏覽器報告及既有 Word／PDF 截圖均屬上一版介面，不作為這次折疊版的視覺驗收。兩份 graph browser 測試已調整選取器，但僅通過語法檢查，尚待重跑。
- 本次僅更新本機介面、程式測試、頁內說明和本指南；尚未提交、推送 GitHub 或部署雲端。

## 前一版路網整合更新（歷史紀錄）

V4 現在有「位置、路網、相機、照片、校正、紀錄」六個底部分頁。「路網」沿用既有地圖編輯器，帶入目前專案與樓層；節點、連線、AR 點位保存後，位置頁從同一後台重新讀取。此次修改尚未推送 GitHub 或部署到 HTTPS 站。

- 本機 API 增加完整專案保存，與照片／校正共用版本衝突檢查、序列化寫入與原子替換；每次成功寫入前保留上一版備份。更新既有節點會保留觀測照片；明確刪除節點則一併移除該節點資料。
- V4 路網草稿與 V3 使用不同瀏覽器快取；切換底部分頁保留同一編輯器。路網尚未保存或正在寫入時，阻止背景照片／校正寫入與場域切換，避免不同版本互相覆蓋。
- 60 項自動測試通過；含原有資料流程及新增完整專案保存、資料保留、空集合、非法路網、版本衝突和備份檢查。
- 17 項原有現場工具瀏覽器流程與 6 項感測／寫入安全流程通過。
- 8 項新增路網瀏覽器流程：非預設專案／樓層；六分頁與內嵌編輯器；節點新增、連線、拖曳與草稿保留；未保存路網阻止背景寫入；AR 點位保存並連通路網；新節點拍照／校正後再次路網保存仍保留資料；409 拒絕但保留可匯出草稿；390px 手機操作與無頁面水平溢出。
- 4 項新增安全瀏覽器流程：非同源／非指定視窗訊息忽略；合法刷新回覆逆序仍保留最新專案；API 故障轉唯讀保留原 iframe 與匯出／重讀能力；空集合可建立專案並由父頁讀回。
- 手機路徑模式點擊放大、縮小與完整顯示，節點數不變；控制區事件不會冒泡成地圖新增節點。最終資產已通過既有同步瀏覽器回歸，測試 Chrome 清理流程也正常結束。
- 本機 8080 已重啟新版服務；`projectWritesEnabled=true`，工作台、編輯器與兩份 JS 資產 HTTP 200 且與最終建置檔案一致。`ar-data.json` 與 `.local/ar-v4/ar-data.json` 的 SHA-256 維持作業前原值。
- 所有寫入測試在隨機隔離埠及暫存資料目錄執行；不向使用者的 8080、GitHub 或外部服務寫入。介面測試與模擬感測器不代表現場定位精度驗收。

新增報告：`test-output/v4-graph/report.json`、`test-output/v4-graph-safety/report.json`；教學截圖明確標示虛構場域／模擬操作。圖解手冊增加路網、平面圖、節點連線、AR 點位與保存驗證章節。

## 前一版 App 式底部分頁更新（歷史紀錄）

依後續需求改為「位置、相機、照片、校正、紀錄」五個固定底部分頁，一次只顯示目前工作區。新增檢查每頁僅一個可見面板、頁籤與面板的無障礙對應、左右鍵／Home／End、手機與桌面底欄固定且可點擊、草稿及未儲存校正值跨頁保留，以及相機離頁暫停／返回恢復。所有影像與資料測試繼續在隔離副本中執行，未變更預設本機現場資料。

各頁的新截圖為 `test-output/v4-tab-{location,camera,capture,calibrate,records}-{mobile,desktop}.png`；以下數量已於推送前重新執行確認，詳細結果見機器可讀報告。

## 前一版已通過

- 一般 AR 後台、V3、V4 生產資產建置，以及 V4 獨立 TypeScript 檢查。
- 53 項自動測試：後台角色／版本衝突／JPEG 驗證／冪等重試、資料持久保存、原始資料保留、感測器來源與拍攝時間、環景投影、V3 預設與 V4 全場景辨識模式。
- 17 項瀏覽器流程：真實專案的隔離副本載入、影像壓縮、分頁切換保留草稿與校正、IndexedDB 草稿重載、實際本機 POST、獨立後台紀錄讀取、節點及明確樓層校正、真實 ORB Worker 自比對、無特徵白圖拒絕、環景透視擷取、切換分頁後重繪、1440px／390px 無頁面水平溢出、五頁底欄可見且不遮擋、鍵盤導覽、拍攝後切換與暫停／恢復、模擬相機拍攝與關閉、無外網請求或原始資料改動。
- 6 項額外瀏覽器安全流程：離開相機分頁取消辨識與暫停串流；模擬相機與未校準羅盤的照片上傳；過期版本拒絕及刷新角度；absolute／relative 切換使箭頭失效；相機關閉移除串流；延遲授權後不在其他分頁啟動相機。
- 既有後台同步瀏覽器回歸：同／不同專案拉取與 IndexedDB 重載維持乾淨狀態、GitHub 最新摘要優先，測試未發送 POST。
- 本機服務 `127.0.0.1:8080` 啟動，工作台 HTTP 200，API 宣告 `storage=local`。

機器可讀測試結果在忽略版控的 `test-output/v4-browser-report.json` 和 `test-output/v4-safety-report.json`；桌面／手機截圖同目錄。

## 未驗收與限制

- 真實手機後置相機、iPhone Safari 感測器行為、場域不同光線／角度／距離下的辨識率、多人併用與大規模資料量，尚未現場驗證。
- 正例使用已保存合成參考圖自比對，是整合測試，不是獨立照片定位準確率。負例使用無特徵白圖，不代表所有相似走廊都不會誤判。
- 本頁僅提供節點候選、人工朝向確認、短暫相對轉動提示；不提供連續公尺距離、六自由度定位、VPS／SLAM 建圖或完整 V4 民眾導覽流程。
- 手機現場連線尚需受保護的 HTTPS 測試環境；本機服務刻意不開放區網、不冒充正式登入。
- 發佈包含既有 AR 後台與 V3 的來源版本檢查更新及重建資產，以配合 API 的 SHA 衝突保護；共用辨識類別新增明確選用的 V4 模式，V3 預設的面積判斷與閾值不變。
- 全專案 TypeScript 檢查仍有舊版 `ar.tsx`／V2 的既存錯誤，未宣稱全庫檢查通過。

## 重跑

```sh
npm run build:ar-v4
npm run typecheck:ar-v4
npm run test:ar-v4
npm run test:ar-v4-browser
```

瀏覽器測試依賴本機已安裝 Chrome 與 Codex bundled Playwright。可透過 `AR_V4_TEST_CHROME`／`AR_V4_TEST_PLAYWRIGHT` 指定替代路徑。
