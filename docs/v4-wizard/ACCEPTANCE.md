# V4 六大功能精靈改造驗證

日期：2026-09-06。範圍：本機 UI、共用儲存 API 相容欄位、既有工具回歸；未推送 GitHub／Azure。

## 三階段交付

1. 共用 APP 外框、手機底部主操作與功能選單、桌面側欄、按需說明面板、依節點保存草稿及恢復步驟。
2. 六大功能日常流程：位置三步、照片四步、校正四步、相機任務分流、路網地圖優先／抽屜工具／獨立樓層選單、紀錄列表與詳細面板。保留完整工具入口。
3. 新環景批次編號與分組、斷線續傳、草稿恢復與衝突確認、操作手冊截圖和回歸驗證。

## 驗證證據

- `npm run build:ar-v4`：成功。
- `npm run typecheck:ar-v4`：成功。
- `npm run test:ar-v4`：75 項通過，包含 API 權限／版本／資料保留，以及新增環景批次欄位。
- `npm run test:ar-v4-browser`：通過。既有完整工具照片、ORB 正反例、感測與相機生命週期；路網新增／拖曳／連線／AR 點／保存／409 衝突／匯出；來源驗證、延遲回應、唯讀失敗、空專案建立等。
- `npm run test:ar-v4-wizard`：通過。11 個精靈整合情境及環景批次回歸，包含：選定節點與草稿重開恢復、照片保存不換 V3 主圖、校正不改歷史照片、恢復的校正需核對新版後台、八方向上傳中斷後重開續傳、不重複、批次詳情、相機退出停止、導航框架移除、樓層獨立切換、360／390／768／1280 CSS 像素六頁無水平溢出、完整工具往返。
- 本機 8080 實際資料以 GET-only 瀏覽器驗證可載入、390px 無水平溢出、無頁面例外；未透過此驗證寫入真實後台。

測試寫入全部使用臨時 loopback 服務與虛構／副本資料。瀏覽器報告與全部截圖在忽略追蹤的 `test-output/`；本資料夾收錄四張可隨手冊分享的合成場域截圖。

## 保留界線

- `ar-v3.html` 與 `assets/ar-v3/` 主程式、CSS、辨識 worker 的 SHA-256 均與改造前一致。
- 原始 `ar-data.json` 雜湊：`3693cdce2b9b91d88ab327a5f33560fb8a304bcb7b303f15f7568288f8d0b2f5`，保持一致。
- 本機 `.local/ar-v4/ar-data.json` 雜湊：`fe6a22415d407d2278b2334d7161e36e58a33a5664a9e56717cd6b7d324ba79b`，驗證前後一致。
- V4 民眾導引已接入當前與下一節點的參考照片候選辨識，但不是全程自動定位；抵達仍由使用者明確確認。
- 真實手機 Safari／Chrome 的相機權限、感測品質、戶外網路中斷與現場定位精度，仍需實機實地驗證。桌面模擬尺寸不是實機認證。
- 資料量大的環景仍受裝置 IndexedDB 空間、解碼記憶體與每節點 24 張限制；原檔應另行備份。

## 同日追加：民眾導引、工具分類與實體比例尺

- 新增 QR 起點、選目的地自動預覽、相機與方向權限、連續照片候選辨識、2D 皮卡方向示意、底部箭頭與 V3 小地圖。看到下一地標不等於抵達；必須按「我已到達」，相機才接續辨識下一段。這不是 3D 空間錨定或行走測距。
- 移除重複的總工具選單按鈕與待機瀏覽文字，以五個分類入口開啟原有全部工具。
- 原本已有實體座標設定；新增整張底圖寬、高公尺輸入和正值驗證。套用到草稿後，仍需核對保存到後台。
- `npm run test:ar-v4-public`：型別檢查及真實 ORB worker 的合成鏡頭測試通過。驗證 A→B→C、候選不自動更新位置、五秒過期、連續鏡頭、確認抵達後停止、無後台寫入與 RWD。
- 路網瀏覽器測試九項及安全測試四項通過，包含零比例尺拒絕、120×80 公尺保存再讀取，以及原節點照片保留。
- 民眾導引追加測試通過：感測角度改變會更新指引箭頭；從轉角返回預覽再進入 AR，保留最後確認節點，退出期間鏡頭已停止。
- 75 項核心測試、完整工具瀏覽器／感測生命週期、六大功能精靈與環景回歸再測通過。V3 發布檔與真實場域資料雜湊仍一致。
- `public-camera.png`、`public-candidate.png`、`map-scale.png` 為合成資料的測試畫面，不是現場定位精度證據；本次僅本機完成，沒有推送 GitHub／Azure。
